# docker/nginx.conf — Configuration Nginx pour MRDPSTOCK
server {
    listen 80;
    server_name _;

    # Rediriger HTTP → HTTPS (décommenter en production avec SSL)
    # return 301 https://$host$request_uri;

    # Headers de sécurité
    add_header X-Frame-Options SAMEORIGIN;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";

    # Limite taille des requêtes (pour les photos base64)
    client_max_body_size 15M;

    # Proxy vers l'application Node.js
    location / {
        proxy_pass http://mrdpstock:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 90s;
    }

    # Cache pour les assets statiques
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        proxy_pass http://mrdpstock:3001;
        proxy_set_header Host $host;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# Configuration HTTPS (décommenter et adapter)
# server {
#     listen 443 ssl http2;
#     server_name votre-domaine.fr;
#
#     ssl_certificate /etc/letsencrypt/live/votre-domaine.fr/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/votre-domaine.fr/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     # Même config location que ci-dessus...
# }
